{{- if and .Values.ceemsAPIServer.enabled .Values.ceemsAPIServer.admissionWebhooks.enabled .Values.ceemsAPIServer.admissionWebhooks.patch.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name:  {{ template "kube-ceems.fullname" . }}-admission-create
  namespace: {{ template "kube-ceems.namespace" . }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
    "helm.sh/hook-weight": "0"
{{- with .Values.ceemsAPIServer.admissionWebhooks.annotations }}
{{ toYaml . | indent 4 }}
{{- end }}
  labels:
    app: {{ template "kube-ceems.name" $ }}-admission-create
    {{- include "kube-ceems.admission.webhook.labels" $ | nindent 4 }}
spec:
  ttlSecondsAfterFinished: {{ .Values.ceemsAPIServer.admissionWebhooks.patch.ttlSecondsAfterFinished }}
  template:
    metadata:
      name:  {{ template "kube-ceems.fullname" . }}-admission-create
{{- with .Values.ceemsAPIServer.admissionWebhooks.patch.podAnnotations }}
      annotations:
{{ toYaml .  | indent 8 }}
{{- end }}
      labels:
        app: {{ template "kube-ceems.name" $ }}-admission-create
        {{- include "kube-ceems.admission.webhook.labels" $ | nindent 8 }}
    spec:
      {{- if .Values.ceemsAPIServer.admissionWebhooks.patch.priorityClassName }}
      priorityClassName: {{ .Values.ceemsAPIServer.admissionWebhooks.patch.priorityClassName }}
      {{- end }}
      {{- if .Values.global.imagePullSecrets }}
      imagePullSecrets:
      {{- include "kube-ceems.imagePullSecrets" . | indent 8 }}
      {{- end }}
      containers:
        - name: create
          {{- $registry := .Values.global.imageRegistry | default .Values.ceemsAPIServer.admissionWebhooks.patch.image.registry -}}
          {{- if .Values.ceemsAPIServer.admissionWebhooks.patch.image.sha }}
          image: {{ $registry }}/{{ .Values.ceemsAPIServer.admissionWebhooks.patch.image.repository }}:{{ .Values.ceemsAPIServer.admissionWebhooks.patch.image.tag }}@sha256:{{ .Values.ceemsAPIServer.admissionWebhooks.patch.image.sha }}
          {{- else }}
          image: {{ $registry }}/{{ .Values.ceemsAPIServer.admissionWebhooks.patch.image.repository }}:{{ .Values.ceemsAPIServer.admissionWebhooks.patch.image.tag }}
          {{- end }}
          imagePullPolicy: {{ .Values.ceemsAPIServer.admissionWebhooks.patch.image.pullPolicy }}
          args:
            - create
            - --host={{- include "kube-ceems.admission.webhook.dnsNames" . | replace "\n" "," }}
            - --namespace={{ template "kube-ceems.namespace" . }}
            - --secret-name={{ template "kube-ceems.fullname" . }}-admission-controller-certs
            - --cert-name=tls.crt
            - --key-name=tls.key
          {{- with .Values.ceemsAPIServer.admissionWebhooks.createSecretJob }}
          securityContext:
          {{ toYaml .securityContext | nindent 12 }}
          {{- end }}
          resources:
{{ toYaml .Values.ceemsAPIServer.admissionWebhooks.patch.resources | indent 12 }}
      restartPolicy: OnFailure
      serviceAccountName: {{ template "kube-ceems.fullname" . }}-admission-webhook
      {{- with .Values.ceemsAPIServer.admissionWebhooks.patch.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
      {{- end }}
      {{- with .Values.ceemsAPIServer.admissionWebhooks.patch.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
      {{- end }}
      {{- with .Values.ceemsAPIServer.admissionWebhooks.patch.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
      {{- end }}
{{- if .Values.ceemsAPIServer.admissionWebhooks.patch.securityContext }}
      securityContext:
{{ toYaml .Values.ceemsAPIServer.admissionWebhooks.patch.securityContext | indent 8 }}
{{- end }}
{{- end }}
