{{- if and .Values.ceemsLB.enabled .Values.ceemsLB.kubeRBACProxy.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "kube-ceems.lb.fullname" . }}-rbac-config
  namespace: {{ include "kube-ceems.lb.namespace" . }}
data:
  config-file.yaml: |+
    authorization:
      resourceAttributes:
        namespace: {{ template "kube-ceems.lb.namespace" . }}
        apiVersion: v1
        resource: services
        subresource: {{ template "kube-ceems.lb.fullname" . }}
        name: {{ template "kube-ceems.lb.fullname" . }}
{{- end }}
