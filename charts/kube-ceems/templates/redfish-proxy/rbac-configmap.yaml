{{- if and .Values.redfishProxy.enabled .Values.redfishProxy.kubeRBACProxy.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "kube-ceems.redfish-proxy.fullname" . }}-rbac-config
  namespace: {{ include "kube-ceems.redfish-proxy.namespace" . }}
data:
  config-file.yaml: |+
    authorization:
      resourceAttributes:
        namespace: {{ template "kube-ceems.redfish-proxy.namespace" . }}
        apiVersion: v1
        resource: services
        subresource: {{ template "kube-ceems.redfish-proxy.fullname" . }}
        name: {{ template "kube-ceems.redfish-proxy.fullname" . }}
{{- end }}
