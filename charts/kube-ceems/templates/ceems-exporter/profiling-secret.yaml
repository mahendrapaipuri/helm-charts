{{- if and .Values.ceemsExporter.enabled .Values.ceemsExporter.eBPFProfiling.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "kube-ceems.exporter.fullname" . }}-profiling-config
  namespace: {{ include "kube-ceems.exporter.namespace" . }}
type: Opaque
data:
  config.yaml: {{ include "kube-ceems.exporter.profiling-config" . | b64enc }}
{{- end }}
